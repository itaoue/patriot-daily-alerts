name: Content pipeline
# Scheduling: the website dispatches this workflow itself at 19:00 America/Los_Angeles (src/scheduler.py, driven by
# GITHUB_DISPATCH_TOKEN on Railway) with guard=true. GitHub's own cron dropped or delayed most runs, so it is not used.
on:
  workflow_dispatch:
    inputs:
      batch_time:
        description: "HH:MM the story batch is scheduled for (the site's SCHEDULE_STORIES); the guard counts stories from this time"
        default: "19:00"
      batch_tz:
        description: "timezone of batch_time (the site's SCHEDULE_TZ)"
        default: "America/Los_Angeles"
      guard:
        description: "true = one batch per day after 19:00 Pacific (set by the site's scheduler)"
        default: "false"
      count:
        description: "Stories to write"
        default: "3"
      status:
        description: "draft or published"
        default: "draft"
      mode:
        description: "write (new stories) or fix-drafts (revise existing drafts per editor notes)"
        default: "write"
concurrency:
  group: content-pipeline
  cancel-in-progress: false
jobs:
  write:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    permissions:
      contents: write
    env:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
      PUBLISH_TOKEN: ${{ secrets.PUBLISH_TOKEN }}
      SITE_URL: ${{ vars.SITE_URL || 'https://patriotdailyalerts.com' }}
      STORY_AUTHOR: ${{ vars.STORY_AUTHOR || 'Joseph Sosa' }}   # byline on pipeline stories; override in repo Variables
      STORY_EFFORT: ${{ vars.STORY_EFFORT || 'medium' }}
      STORY_MODEL: ${{ vars.STORY_MODEL || 'claude-opus-5' }}
      RESEARCH_MODEL: ${{ vars.RESEARCH_MODEL || vars.STORY_MODEL || 'claude-opus-5' }}
      EDITOR_MODEL: ${{ vars.EDITOR_MODEL || vars.STORY_MODEL || 'claude-opus-5' }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip
      - run: pip install -r requirements-content.txt
      - name: Guard (one batch per day, after the scheduled batch time)
        id: guard
        env:
          BATCH_TIME: ${{ github.event.inputs.batch_time }}
          BATCH_TZ: ${{ github.event.inputs.batch_tz }}
        run: |
          if [ "${{ github.event_name }}" != "schedule" ] && [ "${{ github.event.inputs.guard }}" != "true" ]; then echo "skip=false" >> "$GITHUB_OUTPUT"; exit 0; fi
          TARGET=$(python -c "import os, datetime as d, zoneinfo as z; h, m = (os.environ.get('BATCH_TIME') or '19:00').split(':'); n = d.datetime.now(z.ZoneInfo(os.environ.get('BATCH_TZ') or 'America/Los_Angeles')); print(n.replace(hour=int(h), minute=int(m), second=0, microsecond=0).astimezone(d.timezone.utc).strftime('%Y-%m-%dT%H:%M:%S'))")   # today's story-batch time (site scheduler's SCHEDULE_STORIES / SCHEDULE_TZ) as UTC
          NOW=$(date -u +%Y-%m-%dT%H:%M:%S)
          if [[ "$NOW" < "$TARGET" ]]; then echo "before today's batch time ($TARGET UTC); skipping"; echo "skip=true" >> "$GITHUB_OUTPUT"; exit 0; fi
          COUNT=$(curl -s -H "Authorization: Bearer $PUBLISH_TOKEN" -H "User-Agent: Mozilla/5.0" "$SITE_URL/api/posts/recent?days=1" \
            | python -c "import json,sys; print(sum(1 for p in json.load(sys.stdin)['posts'] if p['published_at'] >= '$TARGET'))")
          echo "stories published since $TARGET UTC: $COUNT"
          if [ "$COUNT" -ge 1 ]; then echo "today's batch already ran; skipping"; echo "skip=true" >> "$GITHUB_OUTPUT"; else echo "skip=false" >> "$GITHUB_OUTPUT"; fi
      - name: Topic radar
        if: steps.guard.outputs.skip != 'true'
        run: python tools/radar.py --days 2 --print
      - name: Write stories
        if: steps.guard.outputs.skip != 'true'
        run: |
          if [ "${{ github.event.inputs.mode }}" = "fix-drafts" ]; then python tools/write_stories.py --fix-drafts --status "${{ github.event.inputs.status }}"; exit 0; fi
          if [ -n "${{ github.event.inputs.count }}" ]; then COUNT="${{ github.event.inputs.count }}"; STATUS="${{ github.event.inputs.status }}";
          else COUNT=5; STATUS=published; fi
          python tools/write_stories.py --count "$COUNT" --status "$STATUS"
      - name: Commit images and radar memory
        if: steps.guard.outputs.skip != 'true'
        run: |
          git config user.name "content-bot"
          git config user.email "news@patriotdailyalerts.com"
          # another run may have pushed meanwhile: rebuild on top of origin/main and union-merge the dedupe memory
          cp content/radar/used.json /tmp/used.local.json
          git fetch origin main
          git checkout -- content/radar/used.json
          git checkout -B main origin/main
          python - <<'PY'
          import json
          a = json.load(open("content/radar/used.json")); b = json.load(open("/tmp/used.local.json"))
          for k in ("urls", "titles", "photos"):
              a[k] = list(dict.fromkeys(a.get(k, []) + b.get(k, [])))
          a["photos"] = a["photos"][-60:]
          json.dump(a, open("content/radar/used.json", "w"), indent=1)
          PY
          git add src/static/uploads content/radar/used.json
          git diff --cached --quiet && echo "nothing to commit" || (git commit -m "Content pipeline: images for $(date -u +%F)" && git push origin main)
